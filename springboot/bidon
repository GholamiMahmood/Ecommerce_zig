docker-compose up --build   // la meme place que docker principal exist

docker images // la meme place que docker principal exist, il nous indique les derniere file des images


docker rmi -f <image_id>   // pour suprimmer un images



error:

Error response from daemon: Ports are not available: exposing port TCP 0.0.0.0:3306 -> 0.0.0.0:0: listen tcp 0.0.0.0:3306: bind: Only one usage of each socket address (protocol/network address/port) is normally permitted.
Gracefully stopping... (press Ctrl+C again to force)
